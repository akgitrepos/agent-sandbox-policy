# Architecture Decision Records

This directory stores architecture decision records (ADRs) for ASP.

Guidelines:
- Use sequential numbering: `0001-...`, `0002-...`
- Keep each ADR focused on one decision
- Record context, options considered, and consequences
- Never rewrite history silently; supersede with a new ADR
